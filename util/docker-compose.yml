# docker-compose.yml
version: "3"
services:
  database:
    image: "postgres"
    environment:
      - POSTGRES_USER=cbaccus
      - POSTGRES_PASSWORD=udapeople123
      - POSTGRES_DB=udapeople
    ports:
      - "5532:5432"
